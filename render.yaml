`yaml
services:
  - type: web
    name: omega-99-nuclear
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python omega_bot.py
```